{
    "version": 1,
    "description": "Phase B — Sections persistence round-trip (load half). Loads the project saved by sections_persistence_save.json and asserts all three break points survived the serializer. CTest fixture sections_persistence_data ensures the save script ran first.",
    "commands": [
        {"type": "LoadProject", "filepath": "test_output/sections_persistence/test.entity"},
        {"type": "WaitFrames", "count": 4},
        {"type": "AssertSectionCount", "count": 3},
        {"type": "Exit"}
    ]
}
